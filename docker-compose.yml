services:
  # MySQL Database
  mysql:
    image: mysql:8.0
    container_name: teacher-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: teacherdb
      MYSQL_USER: teacheruser
      MYSQL_PASSWORD: teacherpass
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-proot"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - teacher-network

  # Tomcat Application Server
  tomcat:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: teacher-tomcat
    ports:
      - "8080:8080"
    environment:
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_DATABASE=teacherdb
      - MYSQL_USER=root
      - MYSQL_PASSWORD=root
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - teacher-network

volumes:
  mysql_data:

networks:
  teacher-network:
    driver: bridge

